jenkins:
  version: "2.142"
  files:
    Dockerfile: jenkins/jenkins:**VERSION$$
    README.md: Currently this is built against Jenkins `**VERSION**`.
    Jenkinsfile: docker tag ${dockerImageName} germaniumhq/jenkins:ansible-**VERSION$$

